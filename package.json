{
  "name": "kangni-admin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:html": "vite build --config vite.singlefile.config.ts && python3 scripts/pack_single_html.py",
    "build:html:incentive": "KN_APP=incentive vite build --config vite.app-html.config.ts && python3 scripts/pack_app_html.py incentive",
    "build:html:medal": "KN_APP=medal vite build --config vite.app-html.config.ts && python3 scripts/pack_app_html.py medal",
    "build:html:forum": "KN_APP=forum vite build --config vite.app-html.config.ts && python3 scripts/pack_app_html.py forum",
    "build:html:mailbox": "KN_APP=mailbox vite build --config vite.app-html.config.ts && python3 scripts/pack_app_html.py mailbox",
    "build:html:care": "KN_APP=care vite build --config vite.app-html.config.ts && python3 scripts/pack_app_html.py care",
    "build:html:apps": "npm run build:html:incentive && npm run build:html:medal && npm run build:html:forum && npm run build:html:mailbox && npm run build:html:care",
    "test": "vitest run",
    "preview": "vite preview",
    "check:standards": "python3 scripts/apply_standards.py --config .b2b/b2b-standards.json --check && python3 scripts/check_ui_conformance.py --root .",
    "generate:standards": "python3 scripts/apply_standards.py --config .b2b/b2b-standards.json --out-dir src/shared/design-system/generated"
  },
  "dependencies": {
    "@ant-design/icons": "^6.1.0",
    "antd": "^6.1.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^4.1.10"
  }
}
