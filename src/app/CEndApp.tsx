import { CEndToastProvider } from '../features/c-end/activities/components/CEndToast';
import { H5ActivityDetail } from '../features/c-end/activities/h5/H5ActivityDetail';
import { H5ActivityHome } from '../features/c-end/activities/h5/H5ActivityHome';
import { H5MyFavorites } from '../features/c-end/activities/h5/H5MyFavorites';
import { H5MySignups } from '../features/c-end/activities/h5/H5MySignups';
import { H5SignupPage } from '../features/c-end/activities/h5/H5SignupPage';
import { H5CheckInPage } from '../features/c-end/activities/h5/H5CheckInPage';
import { H5IgCheckInPage } from '../features/c-end/interest-groups/h5/H5IgCheckInPage';
import { PcActivityDetail } from '../features/c-end/activities/pc/PcActivityDetail';
import { PcActivityHome } from '../features/c-end/activities/pc/PcActivityHome';
import { PcMyFavorites } from '../features/c-end/activities/pc/PcMyFavorites';
import { PcMySignups } from '../features/c-end/activities/pc/PcMySignups';
import { H5PastMomentsPage, PcPastMomentsPage } from '../features/c-end/activities/components/PastMomentsPage';
import { H5CourseDetail } from '../features/c-end/courses/h5/H5CourseDetail';
import { H5CourseMall } from '../features/c-end/courses/h5/H5CourseMall';
import { H5CourseNotes } from '../features/c-end/courses/h5/H5CourseNotes';
import { H5CourseNoteDetail } from '../features/c-end/courses/h5/H5CourseNoteDetail';
import { H5ExamList } from '../features/c-end/exams/h5/H5ExamList';
import { H5ExamPrep } from '../features/c-end/exams/h5/H5ExamPrep';
import { H5ExamResult } from '../features/c-end/exams/h5/H5ExamResult';
import { H5ExamRank } from '../features/c-end/exams/h5/H5ExamRank';
import { H5ExamRecords } from '../features/c-end/exams/h5/H5ExamRecords';
import { H5ExamReview } from '../features/c-end/exams/h5/H5ExamReview';
import { H5ExamTaking } from '../features/c-end/exams/h5/H5ExamTaking';
import { H5HonorApp } from '../features/c-end/honor/h5/H5HonorApp';
import { H5IncentiveApp } from '../features/c-end/incentive/h5/H5IncentiveApp';
import { H5DailyCheckinPage } from '../features/c-end/checkin/h5/H5DailyCheckinPage';
import { H5LearningPlanList } from '../features/c-end/learning-plan/h5/H5LearningPlanList';
import { H5PracticeBank } from '../features/c-end/practice/h5/H5PracticeBank';
import { H5PracticeQuiz } from '../features/c-end/practice/h5/H5PracticeQuiz';
import { H5ContestHome } from '../features/c-end/skills-contest/h5/H5ContestHome';
import { H5ContestEvents } from '../features/c-end/skills-contest/h5/H5ContestEvents';
import { H5ContestDocs } from '../features/c-end/skills-contest/h5/H5ContestDocs';
import { H5ContestDetail } from '../features/c-end/skills-contest/h5/H5ContestDetail';
import { H5ContestSignup } from '../features/c-end/skills-contest/h5/H5ContestSignup';
import { H5ContestChallenge } from '../features/c-end/skills-contest/h5/H5ContestChallenge';
import { H5ContestRank } from '../features/c-end/skills-contest/h5/H5ContestRank';
import { H5ContestMine } from '../features/c-end/skills-contest/h5/H5ContestMine';
import { H5ContestWrongBook } from '../features/c-end/skills-contest/h5/H5ContestWrongBook';
import { H5LotteryList } from '../features/c-end/lottery/h5/H5LotteryList';
import { H5LotteryPlay } from '../features/c-end/lottery/h5/H5LotteryPlay';
import { H5InterestGroupHome } from '../features/c-end/interest-groups/h5/H5InterestGroupHome';
import { H5ForumBoard } from '../features/c-end/forum/h5/H5ForumBoard';
import { H5ForumMine } from '../features/c-end/forum/h5/H5ForumMine';
import { H5ForumTopic } from '../features/c-end/forum/h5/H5ForumTopic';
import { H5MailboxHome } from '../features/c-end/forum/h5/H5MailboxHome';
import { H5MailboxDetail } from '../features/c-end/forum/h5/H5MailboxDetail';
import { PcForumBoard } from '../features/c-end/forum/pc/PcForumBoard';
import { PcForumMine } from '../features/c-end/forum/pc/PcForumMine';
import { PcForumTopic } from '../features/c-end/forum/pc/PcForumTopic';
import { PcMailboxHome } from '../features/c-end/forum/pc/PcMailboxHome';
import { PcMailboxDetail } from '../features/c-end/forum/pc/PcMailboxDetail';
import { PcProfileCenter } from '../features/c-end/profile/pc/PcProfileCenter';
import { PcInterestGroupHome } from '../features/c-end/interest-groups/pc/PcInterestGroupHome';
import { H5IgPastMomentsPage, PcIgPastMomentsPage } from '../features/c-end/interest-groups/components/IgPastMomentsPage';
import { H5VoteList } from '../features/c-end/voting/h5/H5VoteList';
import { H5VoteDetail } from '../features/c-end/voting/h5/H5VoteDetail';
import { H5VoteTaking } from '../features/c-end/voting/h5/H5VoteTaking';
import { H5VoteRecords } from '../features/c-end/voting/h5/H5VoteRecords';
import { H5VoteRecord } from '../features/c-end/voting/h5/H5VoteRecord';
import { H5VoteResults } from '../features/c-end/voting/h5/H5VoteResults';
import { H5VoteV2List } from '../features/c-end/voting-v2/h5/H5VoteV2List';
import { H5VoteV2Home } from '../features/c-end/voting-v2/h5/H5VoteV2Home';
import { H5VoteV2OptionDetail } from '../features/c-end/voting-v2/h5/H5VoteV2OptionDetail';
import { H5VoteV2Records } from '../features/c-end/voting-v2/h5/H5VoteV2Records';
import { PcCourseDetail } from '../features/c-end/courses/pc/PcCourseDetail';
import { PcCourseMall } from '../features/c-end/courses/pc/PcCourseMall';
import { PcExamList } from '../features/c-end/exams/pc/PcExamList';
import { PcExamPrep } from '../features/c-end/exams/pc/PcExamPrep';
import { PcExamResult } from '../features/c-end/exams/pc/PcExamResult';
import { PcExamRank } from '../features/c-end/exams/pc/PcExamRank';
import { PcExamRecords } from '../features/c-end/exams/pc/PcExamRecords';
import { PcExamReview } from '../features/c-end/exams/pc/PcExamReview';
import { PcExamTaking } from '../features/c-end/exams/pc/PcExamTaking';
import '../features/c-end/activities/styles.css';
import '../features/c-end/courses/styles.css';
import '../features/c-end/exams/styles.css';
import '../features/c-end/honor/styles.css';
import '../features/c-end/voting/styles.css';
import '../features/c-end/skills-contest/styles.css';
import '../features/c-end/forum/styles.css';
import { CEndEmptyPreviewProvider } from '../features/c-end/portal/emptyPreview';
import type { CEndSurface, H5Page, ProfileSub, ProfileTab } from './navigation';

function voteV2Screens(surface: CEndSurface, h5Page: H5Page | undefined, voteV2Id?: number, voteV2OptionId?: number) {
  if (h5Page === 'votes-v2') return <H5VoteV2List surface={surface} />;
  if (h5Page === 'vote-v2-records') return <H5VoteV2Records surface={surface} />;
  if (h5Page === 'vote-v2-home') return <H5VoteV2Home id={voteV2Id ?? -1} surface={surface} />;
  if (h5Page === 'vote-v2-option') {
    return <H5VoteV2OptionDetail campaignId={voteV2Id ?? -1} optionId={voteV2OptionId ?? -1} surface={surface} />;
  }
  return null;
}

function voteScreens(surface: CEndSurface, h5Page: H5Page | undefined, voteId?: number, voteResponseId?: number) {
  if (h5Page === 'votes') return <H5VoteList surface={surface} />;
  if (h5Page === 'vote-detail') return <H5VoteDetail id={voteId ?? -1} surface={surface} />;
  if (h5Page === 'vote-taking') return <H5VoteTaking id={voteId ?? -1} surface={surface} />;
  if (h5Page === 'vote-results') return <H5VoteResults id={voteId ?? -1} surface={surface} />;
  if (h5Page === 'vote-records') return <H5VoteRecords surface={surface} />;
  if (h5Page === 'vote-record') return <H5VoteRecord id={voteResponseId ?? -1} />;
  return null;
}

type CEndAppProps = {
  surface: CEndSurface;
  activityId?: number;
  courseId?: number;
  examId?: number;
  voteId?: number;
  voteV2Id?: number;
  voteV2OptionId?: number;
  voteResponseId?: number;
  contestId?: number;
  forumBoardId?: number;
  forumTopicId?: number;
  lotteryId?: number;
  practiceCategoryId?: number;
  noteId?: number;
  h5Page?: H5Page;
  empty?: boolean;
  profileTab?: ProfileTab;
  profileSub?: ProfileSub;
};

export function CEndApp(props: CEndAppProps) {
  const { surface, activityId, courseId, examId, voteId, voteV2Id, voteV2OptionId, voteResponseId, contestId, forumBoardId, forumTopicId, lotteryId, practiceCategoryId, noteId, h5Page, empty, profileTab, profileSub } = props;
  const page =
    surface === 'h5' ? (
      h5Page === 'my' ? (
        <H5MySignups />
      ) : h5Page === 'courses' ? (
        <H5CourseMall />
      ) : h5Page === 'course-detail' ? (
        <H5CourseDetail id={courseId ?? -1} />
      ) : h5Page === 'course-notes' ? (
        <H5CourseNotes />
      ) : h5Page === 'course-note-detail' ? (
        <H5CourseNoteDetail id={noteId ?? -1} />
      ) : h5Page === 'favorites' ? (
        <H5MyFavorites />
      ) : h5Page === 'signup' ? (
        <H5SignupPage id={activityId ?? -1} />
      ) : h5Page === 'checkin' ? (
        <H5CheckInPage id={activityId ?? -1} />
      ) : h5Page === 'ig-checkin' ? (
        <H5IgCheckInPage id={activityId ?? -1} />
      ) : h5Page === 'exams' ? (
        <H5ExamList />
      ) : h5Page === 'exam-prep' ? (
        <H5ExamPrep id={examId ?? -1} />
      ) : h5Page === 'exam-taking' ? (
        <H5ExamTaking id={examId ?? -1} />
      ) : h5Page === 'exam-result' ? (
        <H5ExamResult id={examId ?? -1} />
      ) : h5Page === 'exam-review' ? (
        <H5ExamReview id={examId ?? -1} />
      ) : h5Page === 'exam-records' ? (
        <H5ExamRecords id={examId ?? -1} />
      ) : h5Page === 'exam-rank' ? (
        <H5ExamRank id={examId ?? -1} />
      ) : h5Page === 'honor' ? (
        <H5HonorApp />
      ) : h5Page === 'incentive' ? (
        <H5IncentiveApp />
      ) : h5Page === 'daily-checkin' ? (
        <H5DailyCheckinPage />
      ) : h5Page === 'learning-plans' ? (
        <H5LearningPlanList />
      ) : h5Page === 'practice-bank' ? (
        <H5PracticeBank />
      ) : h5Page === 'practice-quiz' ? (
        <H5PracticeQuiz categoryId={practiceCategoryId ?? -1} />
      ) : h5Page === 'contest-home' ? (
        <H5ContestHome />
      ) : h5Page === 'contest-events' ? (
        <H5ContestEvents />
      ) : h5Page === 'contest-docs' ? (
        <H5ContestDocs />
      ) : h5Page === 'contest-detail' ? (
        <H5ContestDetail id={contestId ?? -1} />
      ) : h5Page === 'contest-signup' ? (
        <H5ContestSignup id={contestId ?? -1} />
      ) : h5Page === 'contest-challenge' ? (
        <H5ContestChallenge id={contestId ?? -1} />
      ) : h5Page === 'contest-rank' ? (
        <H5ContestRank id={contestId ?? -1} />
      ) : h5Page === 'contest-mine' ? (
        <H5ContestMine />
      ) : h5Page === 'contest-wrong' ? (
        <H5ContestWrongBook />
      ) : h5Page === 'lottery' ? (
        <H5LotteryList />
      ) : h5Page === 'lottery-play' ? (
        <H5LotteryPlay id={lotteryId ?? -1} />
      ) : h5Page === 'forum' || h5Page === 'forum-board' ? (
        <H5ForumBoard id={forumBoardId ?? 1} />
      ) : h5Page === 'forum-mine' ? (
        <H5ForumMine />
      ) : h5Page === 'forum-topic' ? (
        <H5ForumTopic id={forumTopicId ?? -1} />
      ) : h5Page === 'mailbox-topic' ? (
        <H5MailboxDetail id={forumTopicId ?? -1} />
      ) : h5Page === 'mailbox' ? (
        <H5MailboxHome />
      ) : h5Page === 'interest-groups' ? (
        <H5InterestGroupHome />
      ) : h5Page === 'ig-past-moments' ? (
        <H5IgPastMomentsPage />
      ) : h5Page === 'honor-admin' ? (
        <H5HonorApp initialRole="hr" />
      ) : voteV2Screens('h5', h5Page, voteV2Id, voteV2OptionId) ? (
        voteV2Screens('h5', h5Page, voteV2Id, voteV2OptionId)
      ) : voteScreens('h5', h5Page, voteId, voteResponseId) ? (
        voteScreens('h5', h5Page, voteId, voteResponseId)
      ) : h5Page === 'activity-search' ? (
        <H5ActivityHome variant="search" />
      ) : h5Page === 'activity-list' ? (
        <H5ActivityHome variant="all" />
      ) : h5Page === 'past-moments' ? (
        <H5PastMomentsPage />
      ) : activityId == null ? (
        <H5ActivityHome />
      ) : (
        <H5ActivityDetail id={activityId} />
      )
    ) : h5Page === 'incentive' ? (
      <H5IncentiveApp surface="pc" />
    ) : h5Page === 'profile' ? (
      <PcProfileCenter tab={profileTab} sub={profileSub} />
    ) : h5Page === 'forum' || h5Page === 'forum-board' ? (
      <PcForumBoard id={forumBoardId ?? 1} />
    ) : h5Page === 'forum-mine' ? (
      <PcForumMine />
    ) : h5Page === 'forum-topic' ? (
      <PcForumTopic id={forumTopicId ?? -1} />
    ) : h5Page === 'mailbox-topic' ? (
      <PcMailboxDetail id={forumTopicId ?? -1} />
    ) : h5Page === 'mailbox' ? (
      <PcMailboxHome />
    ) : h5Page === 'courses' ? (
      <PcCourseMall />
    ) : h5Page === 'interest-groups' ? (
      <PcInterestGroupHome />
    ) : h5Page === 'ig-past-moments' ? (
      <PcIgPastMomentsPage />
    ) : h5Page === 'course-detail' ? (
      <PcCourseDetail id={courseId ?? -1} />
    ) : h5Page === 'exams' ? (
      <PcExamList />
    ) : h5Page === 'exam-prep' ? (
      <PcExamPrep id={examId ?? -1} />
    ) : h5Page === 'exam-taking' ? (
      <PcExamTaking id={examId ?? -1} />
    ) : h5Page === 'exam-result' ? (
      <PcExamResult id={examId ?? -1} />
    ) : h5Page === 'exam-review' ? (
      <PcExamReview id={examId ?? -1} />
    ) : h5Page === 'exam-records' ? (
      <PcExamRecords id={examId ?? -1} />
    ) : h5Page === 'exam-rank' ? (
      <PcExamRank id={examId ?? -1} />
    ) : voteV2Screens('pc', h5Page, voteV2Id, voteV2OptionId) ? (
      voteV2Screens('pc', h5Page, voteV2Id, voteV2OptionId)
    ) : voteScreens('pc', h5Page, voteId, voteResponseId) ? (
      voteScreens('pc', h5Page, voteId, voteResponseId)
    ) : h5Page === 'my' ? (
      <PcMySignups />
    ) : h5Page === 'favorites' ? (
      <PcMyFavorites />
    ) : h5Page === 'signup' ? (
      <PcActivityDetail id={activityId ?? -1} signupOpen />
    ) : h5Page === 'activity-search' ? (
      <PcActivityHome variant="search" />
    ) : h5Page === 'activity-list' ? (
      <PcActivityHome variant="all" />
    ) : h5Page === 'past-moments' ? (
      <PcPastMomentsPage />
    ) : activityId == null ? (
      <PcActivityHome />
    ) : (
      <PcActivityDetail id={activityId} />
    );

  return (
    <div className="c-end">
      <CEndEmptyPreviewProvider empty={empty}>
        <CEndToastProvider>{page}</CEndToastProvider>
      </CEndEmptyPreviewProvider>
    </div>
  );
}
