# 勋章应用入口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 管理端「全部应用」挂独立应用「勋章」，左侧一个一级占位菜单，页面走现有 `PlaceholderPage`。

**Architecture:** 只改 `src/app/navigation.ts` 元数据与隐藏页映射。不新建 `features/medal`。`trophy` 已在 `NavIcon` / `navIcons`。`App.tsx` 占位面包屑已用 `siderSelectedKey(page)`，本轮不改。

**Tech Stack:** React 19、antd 6、Vitest、hash 路由 `parseLocationHash` / `toLocationHash`。

**Spec:** `docs/superpowers/specs/2026-09-15-medal-app-design.md`

---

## File map

| Path | Responsibility |
|---|---|
| `src/app/navigation.test.ts` | 应用注册、菜单、hash、顺序、顶栏直显、隐藏页高亮；即时激励顺序断言改为「勋章前」 |
| `src/app/navigation.ts` | `applications`、`applicationMenus.medal`、`extraPages`、`siderSelectedKey` |

不改：`src/app/App.tsx`、`.b2b/b2b-standards.json`、`features/`、C 端、活动勋章库、打卡、评优、荣誉墙。

---

### Task 1: 失败测试

**Files:**
- Modify: `src/app/navigation.test.ts`

- [ ] **Step 1: Write the failing test**

`getApplication` / `applicationMenus` / `applications` / `parseLocationHash` / `getDirectApplications` / `visibleApplications` / `siderSelectedKey` already imported at top of `src/app/navigation.test.ts`. Do not add new imports.

In `describe('incentive application')`, the first `it` currently has:

```ts
    expect(keys.indexOf('incentive')).toBe(keys.indexOf('learning-plan') + 1);
    expect(keys.indexOf('incentive')).toBe(keys.indexOf('care') - 1);
```

Change the second line to:

```ts
    expect(keys.indexOf('incentive')).toBe(keys.indexOf('medal') - 1);
```

Keep `learning-plan + 1`. Instant-激励 stays immediately after 学习计划; 勋章 sits between 即时激励 and 人文关怀.

Append this describe after the existing `incentive application` block (before `experience application after interest-groups split`):

```ts
describe('medal application', () => {
  it('registers 勋章 under 员工与组织 with medal-list as default', () => {
    expect(getApplication('medal')).toEqual({
      key: 'medal',
      label: '勋章',
      category: '员工与组织',
      icon: 'trophy',
      defaultPage: 'medal-list',
    });
    const keys = applications.map((item) => item.key);
    expect(keys.indexOf('medal')).toBe(keys.indexOf('incentive') + 1);
    expect(keys.indexOf('medal')).toBe(keys.indexOf('care') - 1);
  });

  it('uses a single first-level menu for 勋章管理', () => {
    expect(applicationMenus.medal).toEqual([
      { key: 'medal-list', icon: 'trophy', label: '勋章管理' },
    ]);
  });

  it('parses leaf hash and falls back to 勋章管理', () => {
    expect(parseLocationHash('#/medal/medal-list')).toEqual({
      application: 'medal',
      page: 'medal-list',
    });
    expect(parseLocationHash('#/medal')).toEqual({
      application: 'medal',
      page: 'medal-list',
    });
    expect(parseLocationHash('#/medal/not-a-page')).toEqual({
      application: 'medal',
      page: 'medal-list',
    });
  });

  it('parses create, edit and detail hashes and keeps 勋章管理 selected', () => {
    expect(parseLocationHash('#/medal/medal-create')).toEqual({
      application: 'medal',
      page: 'medal-create',
    });
    expect(parseLocationHash('#/medal/medal-edit/2')).toEqual({
      application: 'medal',
      page: 'medal-edit',
      recordId: '2',
    });
    expect(parseLocationHash('#/medal/medal-detail/2')).toEqual({
      application: 'medal',
      page: 'medal-detail',
      recordId: '2',
    });
    expect(siderSelectedKey('medal-create')).toBe('medal-list');
    expect(siderSelectedKey('medal-edit')).toBe('medal-list');
    expect(siderSelectedKey('medal-detail')).toBe('medal-list');
  });

  it('shows 勋章 in 全部应用 switcher but not in top-bar direct apps', () => {
    expect(visibleApplications().map((item) => item.label)).toContain('勋章');
    expect(getDirectApplications(4).map((item) => item.key)).not.toContain('medal');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
npm test -- src/app/navigation.test.ts
```

Expected: FAIL. `getApplication('medal')` is `undefined`. Incentive order assertion fails because `indexOf('medal')` is `-1`. Do not implement yet.

---

### Task 2: 导航元数据与隐藏页

**Files:**
- Modify: `src/app/navigation.ts`

- [ ] **Step 1: Insert the application after `incentive`, before `care`**

In `src/app/navigation.ts`, `applications` currently has:

```ts
  { key: 'incentive', label: '即时激励', category: '员工与组织', icon: 'rocket', defaultPage: 'incentive-list' },
  { key: 'care', label: '人文关怀', category: '员工与组织', icon: 'gift', defaultPage: 'care-plans', hiddenFromSwitcher: true },
```

Change to:

```ts
  { key: 'incentive', label: '即时激励', category: '员工与组织', icon: 'rocket', defaultPage: 'incentive-list' },
  { key: 'medal', label: '勋章', category: '员工与组织', icon: 'trophy', defaultPage: 'medal-list' },
  { key: 'care', label: '人文关怀', category: '员工与组织', icon: 'gift', defaultPage: 'care-plans', hiddenFromSwitcher: true },
```

- [ ] **Step 2: Add menus after `incentive` menus**

`applicationMenus` currently has:

```ts
  incentive: [
    { key: 'incentive-list', icon: 'rocket', label: '激励管理' },
  ],
  care: [
```

Change to:

```ts
  incentive: [
    { key: 'incentive-list', icon: 'rocket', label: '激励管理' },
  ],
  medal: [
    { key: 'medal-list', icon: 'trophy', label: '勋章管理' },
  ],
  care: [
```

- [ ] **Step 3: Register hidden pages in `extraPages`**

In the `extraPages` array in `parseLocationHash`, after `'incentive-detail'`, add:

```ts
    'medal-create',
    'medal-edit',
    'medal-detail',
```

- [ ] **Step 4: Map hidden pages in `siderSelectedKey`**

In `siderSelectedKey`, after the `incentive-create` / `incentive-edit` / `incentive-detail` block, add:

```ts
  if (page === 'medal-create' || page === 'medal-edit' || page === 'medal-detail') {
    return 'medal-list';
  }
```

- [ ] **Step 5: Run tests and confirm they pass**

Run:

```bash
npm test -- src/app/navigation.test.ts
```

Expected: PASS. All `medal application` cases green. Incentive / care tests still pass. `getDirectApplications(4)` still 工作台 / 组织管理 / 商品管理 / 订单管理.

- [ ] **Step 6: Commit**

```bash
git add src/app/navigation.ts src/app/navigation.test.ts
git commit -m "$(cat <<'EOF'
feat: add 勋章 app to 全部应用 switcher

Hang a placeholder admin app under 员工与组织 so operators can switch into 勋章管理 without a feature module yet.
EOF
)"
```

Skip this step if the user has not asked to commit.

---

### Task 3: 手动核对占位页

**Files:** none. `App.tsx` already does `findMenuTrail(sideNodes, siderSelectedKey(page))`.

- [ ] **Step 1: Manual check**

Run `npm run dev`. Open 全部应用 → 员工与组织 → 勋章. Confirm:

- trophy 图标 + 名称「勋章」
- 排在即时激励后、不在顶栏直显四应用里
- 左侧仅「勋章管理」
- 默认 `#/medal/medal-list`，占位文案含「勋章」
- 手动改 hash 为 `#/medal/medal-create`：侧栏仍高亮勋章管理，面包屑为 勋章 > 勋章管理

---

## Spec coverage

| Spec | Task |
|---|---|
| 独立应用 `medal` / 勋章 / 员工与组织 / `trophy` | Task 2 Step 1 |
| 插在即时激励后、人文关怀前 | Task 1 + Task 2 Step 1 |
| 即时激励顺序断言改到勋章前 | Task 1 |
| 不进顶栏直显 | Task 1 last it |
| 一级勋章管理 `medal-list` | Task 2 Step 2 |
| PlaceholderPage、不建 feature | File map（不新增目录） |
| extraPages create/edit/detail | Task 2 Step 3 |
| siderSelectedKey → medal-list | Task 2 Step 4 |
| 隐藏页面包屑勋章管理 | Task 3（App 已接 siderSelectedKey） |
| 测试清单 | Task 1 |
| 不做 CRUD / 活动打卡评优打通 / C 端 / 权限 / 装修 | 无对应实现任务 |
