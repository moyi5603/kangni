# 详情页「调查问卷」文案与入口设计

日期：2026-08-21
状态：已确认并完成

## 改动

1. Tab 上方按钮文案：`满意度调查` → `调查问卷`
2. 点击按钮：`message.info('未开发，跳转到调查问卷')`，再切到 `surveys` tab
3. Tab 文案：`满意度调查` → `调查问卷`（`detailTabs` 与 `Tabs.items`）

key 仍为 `surveys`，路由不变。
